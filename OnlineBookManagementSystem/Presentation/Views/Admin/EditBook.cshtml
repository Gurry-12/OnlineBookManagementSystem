@using OnlineBookManagementSystem.Presentation.ViewModels.Books
@model BookFormViewModel

@{
    ViewData["Title"] = "Edit Book";
    Layout = ViewData["Layout"]?.ToString() ?? "_LayoutAdmin";
}

<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-lg-10 col-xl-8">
            <!-- Glassmorphic Card with Consistent Design -->
            <div class="wp-card shadow-lg">
                <div class="wp-card-header text-center py-4">
                    <h2 class="mb-0 wp-text-gradient">
                        <i class="bi bi-pencil-square me-2"></i>Edit Book
                    </h2>
                    <p class="mb-0 mt-2 ">Update book information: @Model.Book.Title</p>
                </div>
                
                <div class="wp-card-body">
                    <form asp-action="Edit" method="post" enctype="multipart/form-data" class="needs-validation" novalidate>
                        @Html.AntiForgeryToken()
                        <input asp-for="Book.Id" type="hidden" />

                        <partial name="_BookForm" model="Model" />

                        <!-- Action Buttons -->
                        <div class="form__actions form__actions--between pt-4 border-top">
                            <a asp-controller="Admin" asp-action="Books" class="wp-btn wp-btn-secondary">
                                <i class="bi bi-arrow-left me-2"></i>Back to Books
                            </a>
                            
                            <div class="d-flex gap-3">
                                <button type="button" class="wp-btn wp-btn-secondary" onclick="resetForm()">
                                    <i class="bi bi-arrow-clockwise me-2"></i>Reset Changes
                                </button>
                                <button type="submit" class="wp-btn wp-btn-primary">
                                    <i class="bi bi-save me-2"></i>Update Book
                                </button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@await Html.PartialAsync("_Notification")

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $(document).ready(function() {
            // Store original form values for reset functionality
            const originalFormData = new FormData($('form')[0]);
            
            // Image preview functionality
            $('input[type="file"]').on('change', function(e) {
                const file = e.target.files[0];
                const currentImageContainer = $('.current-image-container');
                
                if (file) {
                    const reader = new FileReader();
                    reader.onload = function(e) {
                        // Show preview next to current image
                        let previewHtml = `
                            <div class="new-image-preview mt-3">
                                <label class="wp-form-label">
                                    <i class="bi bi-eye me-1"></i>New Image Preview
                                </label>
                                <div class="new-image-wrapper">
                                    <img src="${e.target.result}" class="wp-book-image" alt="New preview" />
                                    <div class="image-overlay">
                                        <small class="">New Cover</small>
                                    </div>
                                </div>
                            </div>
                        `;
                        
                        // Remove existing preview
                        $('.new-image-preview').remove();
                        currentImageContainer.append(previewHtml);
                    };
                    reader.readAsDataURL(file);
                } else {
                    $('.new-image-preview').remove();
                }
            });

            // Form validation enhancement
            $('form').on('submit', function(e) {
                if (!this.checkValidity()) {
                    e.preventDefault();
                    e.stopPropagation();
                }
                $(this).addClass('was-validated');
            });

            // Reset form function
            window.resetForm = function() {
                if (confirm('Are you sure you want to reset all changes? This will restore the original values.')) {
                    $('form')[0].reset();
                    $('.new-image-preview').remove();
                    $('form').removeClass('was-validated');
                }
            };
        });
    </script>
}

@section Styles {
    
}


