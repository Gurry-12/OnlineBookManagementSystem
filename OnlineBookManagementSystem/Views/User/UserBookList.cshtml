@using OnlineBookManagementSystem.Models.ViewModel
@model BookListViewModel

@{
    ViewData["Title"] = "My Books - User";
    Layout = ViewData["Layout"]?.ToString() ?? "_LayoutUser";
}

<link href="~/css/booksindex.css" rel="stylesheet" />

<div class="container">
    <h2 class="mb-4">📚 Books Management</h2>

    <!-- Search & Filters -->
    <div class="card mb-4 shadow-sm border-0">
        <div class="card-body">
            <form method="get" asp-action="UserBookList">
                <div class="row g-3">
                    <div class="col-md-4">
                        <div class="input-group">
                            <span class="input-group-text bg-white"><i class="bi bi-search"></i></span>
                            <input type="text" name="search" value="@ViewBag.Search" class="form-control" placeholder="Search by title, author..." />
                        </div>
                    </div>
                    <div class="col-md-3">
                        <select name="categoryId" class="form-select" asp-items="ViewBag.Categories">
                            <option value="">All Categories</option>
                        </select>
                    </div>
                    <div class="col-md-2">
                         <select name="sortBy" class="form-select">
                            <option value="">Sort By</option>
                            <option value="priceAsc">Price: Low to High</option>
                            <option value="priceDesc">Price: High to Low</option>
                            <option value="rating">Top Rated</option>
                            <option value="newest">Newest</option>
                        </select>
                    </div>
                     <div class="col-md-3 d-flex gap-2">
                        <button class="btn btn-primary flex-grow-1" type="submit">Filter</button>
                        <a asp-action="UserBookList" class="btn btn-outline-secondary">Reset</a>
                    </div>
                </div>
                <!-- Price Range (Optional Collapsible) -->
                <div class="row g-3 mt-1">
                    <div class="col-md-3">
                        <input type="number" name="minPrice" value="@ViewBag.MinPrice" class="form-control" placeholder="Min Price" />
                    </div>
                    <div class="col-md-3">
                        <input type="number" name="maxPrice" value="@ViewBag.MaxPrice" class="form-control" placeholder="Max Price" />
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Books Grid -->
    <div class="row g-4 mb-4" id="books-list">
        @foreach (var book in Model.Books)
        {
            <div class="col-lg-3 col-md-4 col-sm-6 col-12 p-2">
                <div class="book-card shadow-sm p-2 bg-white rounded position-relative h-100">
                    <div class="position-absolute end-0 top-0 me-1 mt-1 z-3">
                        <button class="btn btn-link p-0" data-bs-toggle="dropdown">
                            <i class="bi bi-three-dots-vertical"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end border-0 shadow z-3">
                            <li><a class="dropdown-item" asp-action="Edit" asp-route-id="@book.Id">Edit</a></li>
                            <li><a class="dropdown-item text-danger" onclick="DeleteBook(@book.Id)">Delete</a></li>
                        </ul>
                    </div>
                    <img src="@(book.ImageUrl ?? Url.Content("~/images/default-book.jpg"))" alt="@book.Title" class="book-image" />
                    <h6 class="mt-2">@book.Title</h6>
                    <small class="text-muted">@book.Author</small>
                    <p class="text-primary fw-bold">₹@book.Price</p>

                    @if (book.StockQuantity > 0)
                    {
                        <small class="text-success fw-bold"><i class="bi bi-check-circle-fill"></i> @book.StockQuantity in stock</small>
                    }
                    else
                    {
                        <small class="text-danger fw-bold"><i class="bi bi-x-circle-fill"></i> Out of Stock</small>
                    }

                    <div class="mt-2">
                        <a asp-action="BookDetails" asp-route-id="@book.Id" class="btn btn-sm btn-outline-primary w-100">View Details</a>
                    </div>
                </div>
            </div>
        }
    </div>

    <!-- Pagination -->
    @if (Model.TotalPages > 1)
    {
        <nav aria-label="Book pagination">
            <ul class="pagination justify-content-center">
                <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
                    <a class="page-link" asp-action="BookList" asp-route-page="@(Model.CurrentPage - 1)">Previous</a>
                </li>
                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    <li class="page-item @(Model.CurrentPage == i ? "active" : "")">
                        <a class="page-link" asp-action="BookList" asp-route-page="@i">@i</a>
                    </li>
                }
                <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
                    <a class="page-link" asp-action="BookList" asp-route-page="@(Model.CurrentPage + 1)">Next</a>
                </li>
            </ul>
        </nav>
    }
</div>

@await Html.PartialAsync("_Notification")

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/js/toastr.min.js"></script>
    <script>
        function DeleteBook(id) {
            if (confirm('Delete this book?')) {
                $.post('/Books/Delete', { id }, function (res) {
                    if (res.success) {
                        toastr.success('Deleted!');
                        location.reload();
                    } else toastr.error('Failed to delete.');
                });
            }
        }
    </script>
}